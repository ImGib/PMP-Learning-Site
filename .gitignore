# Tài liệu nguồn (dung lượng lớn — giữ trên máy, không đưa lên repo)
docs-src/
*.zip
*.pdf

# Claude Code — cho phép commit cấu hình project trong .claude/,
# chỉ bỏ file cục bộ theo máy / runtime
.claude/settings.local.json
.claude/scheduled_tasks.lock

# Thư mục tạm / trích xuất
PMP_Slides_extracted/
txt/

# Astro / Node
node_modules/
dist/
npm-debug.log*

# OS
Thumbs.db
.DS_Store
